<x-theme.app title="{{ $title }}" sizeCard="12"><x-slot name="cardHeader"><h5>Jurnal Penyesuaian</h5><div class="btn-group"><a class="btn btn-primary" href="{{ route('pembukuan-baru.jurnal-penyesuaian.stok-opname') }}">Stok Opname</a><a class="btn btn-outline-primary" href="{{ route('pembukuan-baru.jurnal-penyesuaian.penyusutan-aktiva') }}">Penyusutan Aktiva</a></div></x-slot><x-slot name="cardBody"><form method="POST" action="{{ route('pembukuan-baru.jurnal-penyesuaian.stok-opname.store') }}">@csrf<div class="row mb-3"><div class="col-md-3"><label>Tanggal Opname</label><input type="date" name="tanggal" class="form-control" value="{{ date('Y-m-d') }}" required></div></div><div class="table-responsive"><table class="table table-hover"><thead><tr><th>Item</th><th>Satuan</th><th class="text-end">Stok Sistem</th><th class="text-end">Nilai Satuan</th><th>Stok Fisik</th></tr></thead><tbody>@forelse($items as $i)<tr><td>{{ $i->nama_produk }}<input type="hidden" name="nama_produk[]" value="{{ $i->nama_produk }}"></td><td>{{ $i->satuan }}</td><td class="text-end">{{ number_format($i->qty_masuk,3,',','.') }}<input type="hidden" name="qty_sistem[]" value="{{ $i->qty_masuk }}"></td><td class="text-end">Rp {{ number_format($i->nilai_masuk / max((float)$i->qty_masuk, 0.001),0,',','.') }}<input type="hidden" name="nilai_satuan[]" value="{{ $i->nilai_masuk / max((float)$i->qty_masuk, 0.001) }}"></td><td><input type="number" step="any" name="qty_fisik[]" class="form-control" value="{{ $i->qty_masuk }}" required></td></tr>@empty<tr><td colspan="5" class="text-center text-muted">Belum ada stok dari Pembelian Umum.</td></tr>@endforelse</tbody></table></div>@if($items->isNotEmpty())<button class="btn btn-primary" type="submit"><i class="fas fa-save me-1"></i> Simpan Opname &amp; Buat Jurnal</button>@endif</form></x-slot></x-theme.app>
